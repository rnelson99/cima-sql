CREATE TABLE [project].[proviewFolderStructure] (
    [id]     INT            IDENTITY (1, 1) NOT NULL,
    [folder] VARCHAR (1000) NULL,
    CONSTRAINT [PK_proviewFolderStructure] PRIMARY KEY CLUSTERED ([id] ASC)
);


GO

