CREATE TABLE [accounting].[Deprecated.gltype] (
    [gltypeid] INT          IDENTITY (1, 1) NOT NULL,
    [GLType]   VARCHAR (50) NULL,
    CONSTRAINT [PK_gltype] PRIMARY KEY CLUSTERED ([gltypeid] ASC)
);


GO

