CREATE TABLE [Documents].[attributetypes] (
    [id]            INT           IDENTITY (1, 1) NOT NULL,
    [attributetype] VARCHAR (100) NULL,
    CONSTRAINT [PK_attributetypes] PRIMARY KEY CLUSTERED ([id] ASC)
);


GO

